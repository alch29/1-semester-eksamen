<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  {{> nav title=title backHref="/admin/products"}}
  <div>
      <div class="button1Container">
          <a href="#" class="button1">
            <div class="buttonContent">
              <div>+ Add New Product</div>
            </div>
          </a>
      </div>

    <div>
      {{#each products}}
    <div class="temp-products-container">
      <div class="temp-products">
        {{this.name}}
        <a href="#">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="black" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/>
          <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/>
          </svg>
        </a>
      </div>
    </div>
      {{/each}}
    </div>
  </div>
  <a href="/admin/products" class="modal-backdrop"> </a>
    <div class="modal-container"> 
      <div>
        <h2>Add New Product</h2>   
      </div >
      <form action="/admin/products" method="POST"> 
          <input type="text" class="inputProductName" placeholder="Product name" name="productName" required>
          <div class="modal-chooseUnit">
            <h3>Choose unit</h3> 
            <select id="measurementSelect" name="measurementId" required>
                {{#each measurements}}
                    <option value="{{this.id}}">{{this.measurement_symbol}}</option>
                {{/each}}
            </select>
          </div>

          {{!-- Tag alt fra chooseunit field og product name field, og indsæt i database --}}
          <button type="submit" class="button1">
            <div class="buttonContent">
              <p>Confirm</p>
            </div>
          </button>
        </form>
    </div>

</body>
</html>