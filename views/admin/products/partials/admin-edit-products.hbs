<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <headline class="headerText">Partial for admin-edit-products</headline>
</head>
<body>
        <a href="/admin/products/" > <p><<</p>  </a>
  <div>
    <a href="#" class="button1">
      <div class="buttonContent">
        <div> + </div>
        <div>Add New Product</div>
      </div>
    </a>

    <div>
      {{#each products}}
    <div class="temp-products-container">
      <div class="temp-products">
        {{this.name}}
        <a href="#">Edit</a>
      </div>
    </div>
      {{/each}}
    </div>
  </div>

  <div class="modal-container">
    <div>
      <h2>Edit {{product.name}}</h2>   
    </div >
    <form action="/admin/products/{{product.id}}" method="POST"> 
        <input type="text" class="inputProductName" placeholder="{{product.name}}..." name="name" value="{{product.name}}" required>
        <div class="modal-chooseUnit">
          <h3>Choose unit</h3> 
          <select id="measurementSelect" name="measurementId" required>
              {{#each measurements}}
                  <option value="{{this.id}}" {{#if (eq this.id ../product.measurement_id)}} selected {{/if}}> {{this.measurement_symbol}} </option>
              {{/each}}
          </select>
        </div>

        {{!-- Tag alt fra inputfield og chooseUnit og indsæt i tabellen  --}}
        <button type="submit" class="button1">
          <div class="buttonContent">
            <p>Add New Product</p>
          </div>
        </button>
      </form>
  </div>
  
</body>
</html>